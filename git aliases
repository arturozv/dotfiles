[alias]
	st = status
	co = checkout
	ac = !git add -A && git commit -m
	b = !git checkout -b
	p = !git push origin
	pm = !git checkout main && git pull origin main
	updatefork = !git fetch upstream && git checkout main && git rebase upstream/main
